#include <stdio.h>

/**
  * main - prints all single digit numbers of base ao
  *
  * Return: 0
  */

int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}	
